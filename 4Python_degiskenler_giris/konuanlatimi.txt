---

## 📌 Değişken Nedir?

Değişken, **bir değeri saklamak için kullandığımız isimdir**.
Bilgisayara şunu demiş oluruz:

> “Bu değeri al, şu isimle hatırla.”

---

## 🧠 Python’da Değişken Tanımlama

Python’da değişken tanımlamak çok kolaydır, **tür belirtmeye gerek yoktur**.

```python
x = 10
isim = "Ahmet"
oran = 3.14
```

* `=` → atama operatörü
* Sağdaki değer, soldaki değişkene atanır

---

## 🔠 Değişken Türleri (Otomatik)

Python, değişkenin türünü **kendisi belirler**.

### 1️⃣ Sayılar

```python
sayi = 5        # int
ondalik = 2.5   # float
```

### 2️⃣ Metin (String)

```python
metin = "Merhaba"
isim = 'Ali'
```

### 3️⃣ Boolean (Doğru / Yanlış)

```python
dogru_mu = True
yanlis_mi = False
```

---

## 🔎 Değişkenin Türünü Öğrenme

```python
x = 10
print(type(x))
```

---

## ✏️ Değişken Değeri Değiştirme

```python
x = 10
x = 20
print(x)  # 20
```

👉 Python’da **aynı değişkene farklı türde değer atanabilir**:

```python
x = 5
x = "merhaba"
```

---

## 📐 Değişken İsimlendirme Kuralları

✅ Doğru:

```python
sayi1 = 10
toplam_fiyat = 100
_isim = "Ayşe"
```

❌ Yanlış:

```python
1sayi = 10     # sayı ile başlayamaz
toplam-fiyat = 10  # - kullanılamaz
class = 5      # özel anahtar kelime
```

---

## 🧮 Değişkenlerle İşlemler

```python
a = 10
b = 3

print(a + b)  # toplama
print(a - b)  # çıkarma
print(a * b)  # çarpma
print(a / b)  # bölme
```

---

## 🖨️ Ekrana Yazdırma

```python
isim = "Zeynep"
yas = 20

print(isim, yas)
```

veya:

```python
print(f"İsim: {isim}, Yaş: {yas}")
```

---

## 🎯 Özet

* Değişken = veri saklama alanı
* Python’da tür belirtmeye gerek yok
* Değişkenlerin değeri ve türü değişebilir
* İsimlendirme kurallarına dikkat edilmeli

---

Python’da **değişken kullanırken dikkat edilmesi gerekenler**i kısa, net ve örnekli şekilde özetleyeyim 👇

---

## 1️⃣ Değişken İsimlendirme Kuralları

* Harf, rakam ve `_` (alt çizgi) kullanılabilir
* **Rakamla başlayamaz**
* Türkçe karakter kullanma **önerilmez**
* Büyük/küçük harf **farklıdır**

✅ Doğru:

```python
ogrenci_sayisi = 30
toplamNot = 85
```

❌ Yanlış:

```python
3sayi = 10
öğrenci = "Ali"
toplam-not = 50
```

---

## 2️⃣ Anlamlı İsimler Kullanın

Değişken adı ne tuttuğunu anlatmalı.

❌ Kötü:

```python
x = 120
```

✅ İyi:

```python
urun_fiyati = 120
```

---

## 3️⃣ Python Anahtar Kelimelerini Kullanmayın

Python’un özel kelimeleri değişken adı olamaz.

❌ Yanlış:

```python
if = 10
for = 5
```

Bazı anahtar kelimeler:
`if, else, for, while, class, def, return, True, False`

---

## 4️⃣ Dinamik Tip Yapısını Unutmayın

Python’da değişken tipi sonradan değişebilir ama **dikkatli olun**.

```python
x = 10      # int
x = "Ali"   # str
```

Bu esneklik bazen hataya yol açabilir ⚠️

---

## 5️⃣ Sabitler Büyük Harfle Yazılır (Gelenek)

Python’da gerçek sabit yoktur ama **büyük harf** ile yazılır.

```python
PI = 3.14
MAX_KULLANICI = 100
```

---

## 6️⃣ Aynı Değişkeni Farklı Amaçla Kullanmayın

Kod okunabilirliği düşer.

❌ Kötü:

```python
x = 5
x = "Merhaba"
```

---

## 7️⃣ Boşluklara ve Okunabilirliğe Özen Gösterin

```python
toplam_not = vize + final
ortalama = toplam_not / 2
```

---

## 8️⃣ Global Değişkenleri Az Kullanın

Fonksiyon içinde kullanılan değişkenler **yerel** olmalı.

```python
def hesapla():
    toplam = 10
    return toplam
```

---
